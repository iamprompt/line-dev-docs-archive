---
title: Why can't I open the LIFF browser when I access the LIFF URL?
navigation: true
description: >-
  In some cases, when you access a LIFF URL on an external browser such as
  Safari or Chrome, universal links or app links may not work and the LIFF
  browser may not open on the LINE app. This depends on the specifications of
  the user's OS. In addition, when accessing a LIFF URL on a native app other
  than the LINE app, whether the LIFF app is opened in the external browser or
  the LIFF browser depends on the WebView specifications of the native app.
meta: >-
  {"date":"2025-04-25 00:00 UTC","tags":"liff, line-mini-app,
  line-app","priority":1000,"locale":"en"}
path: /en/_partials/faq/why-cant-i-open-the-liff-browser-when-i-access-the-liff-url
__hash__: VLd1brhleqSTMByYEbAaVYSm4WvJAoE5BeqVouEPzHE
seo:
  title: Why can't I open the LIFF browser when I access the LIFF URL?
  description: >-
    In some cases, when you access a LIFF URL on an external browser such as
    Safari or Chrome, universal links or app links may not work and the LIFF
    browser may not open on the LINE app. This depends on the specifications of
    the user's OS. In addition, when accessing a LIFF URL on a native app other
    than the LINE app, whether the LIFF app is opened in the external browser or
    the LIFF browser depends on the WebView specifications of the native app.
---

In some cases, when you access a LIFF URL on an external browser such as Safari or Chrome, [universal links](https://developer.apple.com/documentation/xcode/allowing-apps-and-websites-to-link-to-your-content/){rel="[\"nofollow\"]"} or [app links](https://developer.android.com/training/app-links){rel="[\"nofollow\"]"} may not work and the LIFF browser may not open on the LINE app. This depends on the specifications of the user's OS. In addition, when accessing a LIFF URL on a native app other than the LINE app, whether the LIFF app is opened in the external browser or the LIFF browser depends on the WebView specifications of the native app.

For these reasons, we don't guarantee in which environment the LIFF app will open when the LIFF URL is accessed.

For more information, see [Environment where the LIFF app opens when a user access the LIFF URL](/docs/liff/opening-liff-app/#environment-in-which-liff-opens) in the LIFF documentation.
