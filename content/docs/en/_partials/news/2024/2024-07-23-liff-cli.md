---
title: LIFF CLI, a CLI tool to help you develop LIFF apps more smoothly, released
navigation: true
description: ''
meta: '{"date":"2024-07-23 00:00 UTC","tags":"liff","locale":"en"}'
path: /en/_partials/news/2024/2024-07-23-liff-cli
__hash__: x3uKRESJnVCvljysymsiasQKPmNcsDlD-uJZx8xqigQ
seo:
  title: LIFF CLI, a CLI tool to help you develop LIFF apps more smoothly, released
  description: ''
---

### What is the LIFF CLI

[LIFF CLI](/docs/liff/liff-cli/) is a CLI tool to help you develop LIFF apps more smoothly. The LIFF CLI allows you to create, update, list, and delete LIFF apps from the command line.

- [LIFF CLI](https://www.npmjs.com/package/@line/liff-cli){rel="[\"nofollow\"]"}

In addition, [Create LIFF App](/docs/liff/cli-tool-create-liff-app/), [LIFF Inspector](/docs/liff/liff-plugin/#liff-inspector), and [LIFF Mock](/docs/liff/liff-plugin/#liff-mock) functions will be added in future updates.

### Install the LIFF CLI

Open a terminal or command line tool (hereafter referred to as "terminal") and execute the following command:

```bash
$ npm install -g @line/liff-cli
```

The command will install the LIFF CLI and allow you to run the `liff-cli` command.

### Sample code

For example, to create a LIFF app with a LIFF app name of "Brown Coffee", an endpoint URL of "[https]://example.com", and a view size of "Full", execute the following command:

```bash
$ liff-cli app create \
  --name "Brown Coffee" \
  --endpoint-url https://example.com \
  --view-type full
```

For more information about the LIFF CLI, see [LIFF CLI](/docs/liff/liff-cli/) in the LIFF documentation.
