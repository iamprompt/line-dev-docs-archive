---
title: We've released LIFF v2.11.1
navigation: true
description: We've released LIFF v2.11.1.
meta: '{"date":"2021-06-24 00:00 UTC","tags":"LIFF, LINE MINI app","locale":"en"}'
path: /en/_partials/news/2021/2021-06-24-liff-v2-11-1-release
__hash__: hnl5BBR_kfeYrPswMf9tUy6KFxWKNahczR25tv84hNY
seo:
  title: We've released LIFF v2.11.1
  description: We've released LIFF v2.11.1.
---

We've released LIFF v2.11.1.

In LIFF v2.11.1, this bug has been fixed.

### We fixed the bug that caused URL fragments to be URL-encoded after the LIFF app is initialized

In [LIFF v2.11.0](/docs/liff/release-notes/#liff-v2-11-0), when you access LIFF URLs containing URL fragments (e.g. `#url-fragment`), there was a bug that caused the URL fragments to be URL-encoded after the LIFF app is initialized (after [`liff.init()`](/reference/liff/#initialize-liff-app) is executed).

We've fixed this bug in LIFF v2.11.1 so that even after `liff.init()` URL fragments aren't URL-encoded.

#### Example of URL fragment after liff.init()

In the LIFF v2.11.0 example below, the slash (`/`) in the URL fragment (`#url/fragment`) is URL-encoded (`%2F`).

| LIFF version | LIFF URL                                                                                                            | URL after `liff.init()`                                                                                               |
| ------------ | ------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| v2.11.0      | [https]://liff.line.me/{liffId}:b[#url]{style="color:blue"}:b[/]{style="color:red"}:b[fragment]{style="color:blue"} | [https]://liff.line.me/{liffId}:b[#url]{style="color:blue"}:b[%2F]{style="color:red"}:b[fragment]{style="color:blue"} |
| v2.11.1      | [https]://liff.line.me/{liffId}:b[#url]{style="color:blue"}:b[/]{style="color:red"}:b[fragment]{style="color:blue"} | [https]://liff.line.me/{liffId}:b[#url]{style="color:blue"}:b[/]{style="color:red"}:b[fragment]{style="color:blue"}   |

::admonition{title="We recommend updating to LIFF v2.11.1" type="note"}
In LIFF v2.11.0 of the LIFF app, the bug occurs regardless of browser type (:glossary-tooltip[[LIFF browser](/glossary/#liff-browser)]{glossary-id="liff-browser"}, :glossary-tooltip[[LINE's in-app browser](/glossary/#line-iab)]{glossary-id="line-iab"}, :glossary-tooltip[[external browser](/glossary/#external-browser)]{glossary-id="external-browser"}). The same bug also occurs when you directly access not only a LIFF URL (e.g. `https://liff.line.me/{liffId}/#url/fragment`), but also an endpoint URL (e.g. `https://example.com/#url/fragment`).

If you're using v2.11.0, we recommend that you update to v2.11.1 to avoid unintended behaviors.
::

For more information on how to integrate the LIFF SDK, see [Integrating the LIFF SDK with the LIFF app](/docs/liff/developing-liff-apps/#integrating-sdk).
