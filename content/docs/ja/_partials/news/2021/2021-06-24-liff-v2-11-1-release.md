---
title: LIFF v2.11.1をリリースしました
navigation: true
description: LIFF v2.11.1をリリースしました。
meta: '{"date":"2021-06-24 00:00 UTC","tags":"LIFF, LINE MINI app","locale":"ja"}'
path: /ja/_partials/news/2021/2021-06-24-liff-v2-11-1-release
__hash__: fGrKynhr-UVmoZF1icmzNObRJKZ5791SzVEmqZwJcxY
seo:
  title: LIFF v2.11.1をリリースしました
  description: LIFF v2.11.1をリリースしました。
---

LIFF v2.11.1をリリースしました。

LIFF v2.11.1では以下の不具合を修正しています。

### LIFFアプリ初期化後にURLフラグメントがURLエンコードされる不具合を修正しました

[LIFF v2.11.0](/docs/liff/release-notes/#liff-v2-11-0)では、URLフラグメント（例：`#url-fragment`）を含むLIFF URLにアクセスした際に、LIFFアプリ初期化後（[`liff.init()`](/reference/liff/#initialize-liff-app)実行後）にURLフラグメントがURLエンコードされる不具合がありました。

LIFF v2.11.1では、`liff.init()`後もURLフラグメントがURLエンコードされないように不具合を修正しました。

#### liff.init()後のURLフラグメントの例

以下の例では、LIFF v2.11.0は`liff.init()`後に、URLフラグメント（`#url/fragment`）内のスラッシュ（`/`）がURLエンコード（`%2F`）されています。

| LIFFバージョン | LIFF URL                                                                                                            | `liff.init()`後のURL                                                                                                    |
| --------- | ------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| v2.11.0   | [https]://liff.line.me/{liffId}:b[#url]{style="color:blue"}:b[/]{style="color:red"}:b[fragment]{style="color:blue"} | [https]://liff.line.me/{liffId}:b[#url]{style="color:blue"}:b[%2F]{style="color:red"}:b[fragment]{style="color:blue"} |
| v2.11.1   | [https]://liff.line.me/{liffId}:b[#url]{style="color:blue"}:b[/]{style="color:red"}:b[fragment]{style="color:blue"} | [https]://liff.line.me/{liffId}:b[#url]{style="color:blue"}:b[/]{style="color:red"}:b[fragment]{style="color:blue"}   |

::admonition{title="LIFF v2.11.1へのアップデートをおすすめします" type="note"}
LIFF v2.11.0を利用したLIFFアプリの場合、ブラウザの種類（:glossary-tooltip[[LIFFブラウザ](/glossary/#liff-browser)]{glossary-id="liff-browser"}、:glossary-tooltip[[LINE内ブラウザ](/glossary/#line-iab)]{glossary-id="line-iab"}、:glossary-tooltip[[外部ブラウザ](/glossary/#external-browser)]{glossary-id="external-browser"}）に限らず本不具合が発生します。また、LIFF URL（例：`https://liff.line.me/{liffId}/#url/fragment`）だけでなく、エンドポイントURL（例：`https://example.com/#url/fragment`）に直接アクセスした場合も同様に本不具合が発生します。

意図しない挙動を避けるため、LIFF v2.11.0を利用している場合は、v2.11.1へのアップデートをおすすめします。
::

LIFF SDKの組み込み方法について詳しくは、「[LIFFアプリにLIFF SDKを組み込む](/docs/liff/developing-liff-apps/#integrating-sdk)」を参照してください。
